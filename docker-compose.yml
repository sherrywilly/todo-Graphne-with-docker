version: '3'

services: 
    web:
        build: .
        command: python manage.py runserver 0.0.0.0:$PORT
        volumes: 
            - .:/app
        ports: 
            - "$PORT:$PORT"
        
